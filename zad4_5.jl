e = MathConstants.e
# println(e^0.5)
n = readline()
n = parse(Int64, n)
x_array = zeros(0)
y_array = zeros(0)

for i = 1:n
    x = readline()
    x = parse(Float64, x)
    push!(x_array, x)
end
for i = 1:n
    y = readline()
    y = parse(Float64, y)
    push!(y_array, y)
end

# println(x_array)
# println(y_array)

for i = 1:n
    for j = 1:n

    end
end
